# Go (verb)
The `go_` verb is the command to inert momentum in the proceeding object.

## Syntax

&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; `go_`*`<object>`*`(`*`….`*`)`*`…`*

```diego
go_robot(alif)_wp(wp1);
```

